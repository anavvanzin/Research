# Audit Report — Architecture — Agent 2.2
Escopo examinado: workspace topology (/Users/ana/Research)
Data: 2026-04-17

## Resumo executivo

A topologia declarada em `AGENTS.md` está **100% íntegra no nível de symlinks**
— nenhum link quebrado, nenhum ciclo. Todos os 8 symlinks canônicos declarados
resolvem para alvos existentes. **Porém**, a integridade estrutural contrasta
com três falhas arquiteturais severas:

1. **Contrato do corpus violado em quatro frentes simultâneas**: existem
   **4 cópias físicas** de `corpus-data.json` com contagens divergentes
   (89 / 145 / 165 / 165). O snapshot HF (145) e o hub vivo (165) já não
   coincidem com o número canônico declarado em AGENTS.md (“145 itens”).
2. **`companion-data.json` não é o corpus transformado** — é um payload de
   *summary/statistics* com 5 chaves (`generated`, `corpus_total`, `vault`,
   `countries`, `zwischenraum_panels`); não cabe a afirmação do AGENTS de
   que “apps leem corpus-data.json / companion-data.json”. A companion lê
   `corpus-data.json` **direto do hub** via caminho absoluto.
3. **CI cobre apenas 2 dos 8 repos ativos** (hub + Atlas). Companion,
   iurisvision, iconocracia-space, corpus-scout-agent, gallica-mcp-server
   e o meta-workspace **não têm workflow funcional**. O único workflow
   em `~/Research/.github/workflows/` é o template `blank.yml` intocado.

Além disso, três diretórios gordos dentro do hub são **órfãos operacionais**
(sem referência executável): `postman/` (55M, gitignorado), `PHD/` (88K),
e `Bibliografia - Uso simbólico/` (2.0M, explicitamente no `.gitignore`).

O import cross-repo de `apps → hub` via caminho absoluto hardcoded
(`sync-thesis-content.mjs`) e o acoplamento da companion ao Atlas por
**cópia estática triplicada** (já reportado pelo Agent 1.2) agravam o
risco: mudanças no hub não propagam automaticamente para a companion.

## Achados — CRITICAL

**C1. Caminho absoluto hardcoded em `apps/iconocracia-companion`**
Arquivo `scripts/sync-thesis-content.mjs` linha 17:
```
const DEFAULT_CORPUS_PATH = "/Users/ana/Research/hub/iconocracy-corpus/corpus/corpus-data.json";
```
Quebra portabilidade, CI em ambiente Linux e qualquer colaboração externa.
Deveria ser relativo via `path.resolve(__dirname, "../../../hub/...")` ou
variável de ambiente.

**C2. Quatro versões de `corpus-data.json` em disco — drift real**
| Caminho | Itens | Bytes |
|---|---|---|
| `hub/iconocracy-corpus/corpus/corpus-data.json` | 165 | 573.192 |
| `hub/iconocracy-corpus/output/huggingface/2026-04-06/corpus-data.json` | 145 | 542.019 |
| `hub/iconocracy-corpus/.claude/worktrees/research-permanent/corpus/corpus-data.json` | 165 | 573.192 |
| `archive/iconocracy-corpus-legacy/corpus/corpus-data.json` | 89 | 163.450 |

O **número canônico declarado é 145** (AGENTS.md workspace + AGENTS.md
pessoal do usuário). O hub vivo tem 165. O snapshot HuggingFace tem 145.
Não há nenhum processo de reconciliação documentado entre 165→145.

**C3. Companion NÃO tem CI**
`apps/iconocracia-companion/.github/` não existe. Repo sem `.gitignore`
(reportado pelo Agent 1.2) + sem CI + caminho absoluto hardcoded = pipeline
de deploy manual, sem gate de validação.

**C4. Meta-workspace CI é placeholder**
`.github/workflows/blank.yml` é o template zerado da GitHub
(“This is a basic workflow to help you get started with Actions”).
Nenhuma validação efetiva no root.

## Achados — MAJOR

**M1. Cópia triplicada do Atlas já conhecida — confirmada topologicamente**
`pipelines/Atlas/atlas_iconocratico/` (canônico) vs
`apps/iconocracia-companion/atlas-iconocratico-toolkit/atlas_iconocratico/`
(fork com conteúdo divergente). Ambas importam `from atlas_iconocratico.*`
dentro do próprio diretório, portanto divergência silenciosa.

**M2. Symlinks relativos a caminhos FORA de `Research/`**
`hub/iconocracy-corpus/tools/scripts/{txt, kw, notations.txt, keys.txt}` →
apontam para `/Users/ana/iconclass-data/…` — caminho HOME antigo (pré-
migração `~/Research/`). Não documentados no AGENTS.md. Se `~/iconclass-data`
for removido, quebram silenciosamente scripts de iconclass. Deveriam
apontar para `shared/iconclass-data/…`.

**M3. Worktree ativa em `.claude/worktrees/research-permanent/`**
Contém replica completa do hub (inclusive `corpus-data.json` com 165 itens)
dentro de `.claude/`. Aumenta área de superfície de drift (se alguém editar
por engano o worktree em vez do hub). Não documentada no AGENTS.md.

**M4. CI do hub valida apenas schema, não corpus-data.json**
`hub/.github/workflows/validate.yml` é acionado por `corpus/**`,
`data/processed/**`, `tools/schemas/**` — mas Agent 1.1 mostrou que há
54/165 itens sem citation_abnt, 19/165 sem panofsky. O gate atual **não**
valida completude de campos, apenas shape de JSON Schema.

**M5. Atlas CI é irrelevante**
`pipelines/Atlas/.github/workflows/python-package.yml` roda pytest em
Python 3.9/3.10/3.11 — mas o Atlas não tem `tests/` significativos (era
template-padrão GitHub). `python-package-conda.yml` faz `flake8` ampla
(não configurado para o projeto). Ambos são herança do template Actions.

**M6. `shared/iconclass-data` e `shared/iconclass-data-avmadrj` têm CI
idêntico mas divergente do upstream**
Mesmo workflow (`github-actions-releasedata.yml`), usando `actions/checkout@v3`
e `actions/upload-artifact@v3` (depreciados — GitHub já desativou v3
uploads). CI quebrado na prática.

**M7. React 19 em iurisvision vs ausência em companion**
labs/iurisvision usa React 19.0.0 + Vite 6.2 + TypeScript 5.8.2.
Companion não usa React — é esbuild IIFE bundle puro (segundo seu AGENTS.md).
**Não há drift real** nesse eixo, mas há inconsistência de stack — a
“companion” não compartilha nenhum padrão UI com os outros apps.

## Achados — MINOR

**m1. `.devcontainer/` no root do workspace mas sem documentação**
Existe `/Users/ana/Research/.devcontainer/` (1 entrada) — não referenciado
em AGENTS.md nem README.md.

**m2. `AGENTS.md` pessoal do usuário (~) desatualizado**
Lista paths flat (`~/iconocracy-corpus`, `~/Projects/...`) e contradiz
a migração para `~/Research/`. Confunde agentes novos.

**m3. `Books/`, `Cotutela/`, `rotinas/` no root do workspace**
Não documentados na tabela de buckets canônicos (`hub|apps|pipelines|
labs|vaults|shared|archive`) do AGENTS.md. `rotinas/templates` é symlink
para `hub/.../vault/_templates`.

**m4. Archive references legítimas**
`archive/js-genai` é referenciado via symlink ativo
`hub/iconocracy-corpus/js-genai`. AGENTS.md diz que archive é “read-only por
convenção”, mas o hub depende ativamente dele via symlink — contradição
documental.

## Topologia real vs declarada

- **Symlinks válidos (todos OK):** 9 documentáveis
  - `pipelines/iconocracy-ingest` → `hub/iconocracy-corpus/iconocracy-ingest` ✓
  - `vaults/iconocracy-vault` → `hub/.../vault` ✓
  - `vaults/dir410340-vault` → `hub/.../vault/obsidian-dir410340` ✓
  - `vaults/dir410346-vault` → `hub/.../vault/obsidian-dir410346` ✓
  - `hub/.../Atlas` → `pipelines/Atlas` ✓ (reverso)
  - `hub/.../indexing` → `pipelines/indexing` ✓ (reverso)
  - `hub/.../iurisvision` → `labs/iurisvision` ✓ (reverso, NÃO documentado no AGENTS)
  - `hub/.../js-genai` → `archive/js-genai` ✓ (documentado em docs/workspace-map.md, não em AGENTS.md)
  - `rotinas/templates` → `hub/.../vault/_templates` ✓ (não documentado)

- **Symlinks quebrados:** NENHUM

- **Symlinks circulares:** NENHUM

- **Symlinks não documentados no AGENTS.md do root:**
  1. `hub/iconocracy-corpus/iurisvision` → `labs/iurisvision` (reverso)
  2. `hub/iconocracy-corpus/js-genai` → `archive/js-genai`
  3. `rotinas/templates` → `hub/.../vault/_templates`
  4. `hub/iconocracy-corpus/tools/scripts/{txt,kw,notations.txt,keys.txt}` →
     `/Users/ana/iconclass-data/…` (caminho externo legado)
  5. `hub/iconocracy-corpus/.claude/worktrees/research-permanent/{iurisvision,
     indexing,js-genai,Atlas}` (worktree replica)

- **Symlinks documentados mas ausentes:** NENHUM — a declaração está íntegra
  no que foi afirmado.

## Grafo de dependências cross-repo

**Quem lê `corpus-data.json` (consumidores reais, excluindo docs/logs):**
- `apps/iconocracia-space/app.py` (lê do dataset HF público)
- `apps/iconocracia-companion/scripts/sync-thesis-content.mjs` (caminho
  absoluto para hub — **violação de portabilidade**)
- `apps/iconocracia-companion/scripts/seed-kv.js` (relativo + fallback
  `../../iconocracy-corpus/corpus/corpus-data.json`)
- `labs/iurisvision/src/data/roadmap.ts` (apenas string de status UI —
  não lê arquivo, mas referencia o path)
- `hub/iconocracy-corpus/iconocracy-ingest/modules/corpus_bridge.py`
- `hub/iconocracy-corpus/tests/argos/test_manifest_builder.py`
- `hub/iconocracy-corpus/corpus/infografico_corpus.py`
- `hub/iconocracy-corpus/tmp/pdfs/generate_iconocracy_corpus_ui_summary.py`

**Quem lê `records.jsonl`:**
- `hub/iconocracy-corpus/tools/scripts/{vault_sync,reconcile_data,
  reconcile_iconocode,build_hf_release,build_iconocracy_sft_dataset,
  mcp_integration,corpus_pipeline.sh}.py|sh`
- `apps/iconocracia-companion/src/app/main.jsx` (apenas string UI)
- `apps/iconocracia-companion/public/app.js` (build artifact)
- `apps/iconocracia-companion/CLAUDE.md`, AGENTS.md (docs)
- `shared/iconclass-data/batch_example.py`
- `shared/iconclass-data-avmadrj/batch_example.py`
- `shared/iconclass-data/evaluation/*` (3 arquivos)

**Quem lê `companion-data.json`:**
- Apenas o próprio hub (vault/sessoes/SCOUT-SESSION-2026-03-31b.md)
  e o skill `sync-corpus/SKILL.md`.
- Shape: `{generated, corpus_total, vault, countries, zwischenraum_panels}`
  — payload de **resumo/estatística** de 5 chaves, **não** lista de itens.
- **Conclusão**: o nome sugere “transformação do corpus para companion”,
  mas o arquivo NÃO é consumido pela companion — é só um dump de métricas
  gerado por `sync-corpus` no hub. **Nome enganoso.**

**Violações de direção:**
- `apps/iconocracia-companion/scripts/sync-thesis-content.mjs` → lê hub
  por **caminho absoluto** (`/Users/ana/Research/hub/…`). Direção correta
  (apps consome hub), mas mecanismo quebrado.
- `apps/iconocracia-companion/scripts/seed-kv.js` → usa caminho relativo
  `../../iconocracy-corpus/…` que assume layout flat antigo (`~/`),
  **não** a topologia `Research/`. Quebraria em ambiente com layout
  canônico.
- `hub/` **NÃO** importa de `apps/` nem `labs/` — direção preservada.
- `apps/iconocracia-companion` **NÃO** importa de `pipelines/Atlas` via
  Python; em vez disso, **copiou** `atlas_iconocratico/` para dentro de
  seu próprio toolkit (já reportado pelo Agent 1.2 como duplicação).

## Zombie code em archive/

Busca por referências aos 6 repos arquivados, **excluindo** o próprio
`archive/` e `.git/`:

| Repo arquivado | Referências fora de archive/ |
|---|---|
| `archive/js-genai` | **Symlink ativo** em `hub/iconocracy-corpus/js-genai` + 2 docs (`README.md`, `docs/workspace-map.md`) |
| `archive/iconocracy-corpus-legacy` | Apenas tabelas de documentação (README + workspace-map) |
| `archive/iurisvision-home-duplicate` | Apenas README |
| `archive/iurisvision-projects-duplicate` | Apenas README |
| `archive/webiconocracy-retired` | Nenhuma referência fora de archive/ ✓ |
| `archive/root-vault-legacy` | Apenas README |
| `archive/scispace-old-outputs` | Apenas README |

**Zumbi real**: apenas `archive/js-genai` — ainda tem symlink ativo no
hub (`hub/iconocracy-corpus/js-genai`). Ou o symlink deveria ser removido,
ou o repo deveria sair do archive/ e virar `shared/js-genai`.

## Órgãos (candidatos a mover para archive)

Scan de diretórios grandes dentro do hub com baixa contagem de referências:

| Dir | Tamanho | Refs operacionais | Status |
|---|---|---|---|
| `postman/` | 55M | 1 (self-ref) | **ÓRFÃO** — SDKs Postman gerados, nunca importados |
| `deploy/` | 62M | 22 | Ativo (deploy iconocracia-companion mirror) |
| `gallery/` | 6.4M | 18 | Ativo |
| `shared/` | 181M | 26 | Ativo (mas nome conflita com `~/Research/shared/`) |
| `Bibliografia - Uso simbólico/` | 2.0M | 1 (self-ref) | **GITIGNORADO** — dump local, órfão do repo |
| `Notas e Textos/` | 12K | 5 (2 prompts, .gitignore) | **GITIGNORADO** — dump local, 2 prompts citam fontes |
| `concepts/` | 36K | 12 | Ativo |
| `entities/` | 4.0K | 8 | Ativo |
| `biblio/` | 4.0K | 50 | Ativo |
| `PHD/` | 88K | 3 | Baixo uso — candidato a revisão |
| `examples/` | 32K | 10 | Ativo |

**Recomendação**: `postman/` (55M!) → `archive/postman-sdks/`; documentar
claramente a retirada em `README.md`. `Bibliografia - Uso simbólico/` e
`Notas e Textos/` → mover para `~/Notebooks/` ou pasta local fora do repo,
já que estão gitignorados.

## CI coverage table

| workflow file | repo | triggers | what it validates |
|---|---|---|---|
| `Research/.github/workflows/blank.yml` | meta-workspace | push/PR main, workflow_dispatch | **NADA** — template GitHub intocado, só ecoa “Hello, world!” |
| `hub/.github/workflows/validate.yml` | hub | push/PR em `corpus/**`, `data/processed/**`, `tools/schemas/**`, `tools/scripts/**` | Python 3.11 + `validate_schemas.py` (shape JSON Schema apenas — não valida completude de campos) |
| `hub/.github/workflows/deploy.yml` | hub | push main + dispatch | GitHub Pages static deploy |
| `hub/.github/workflows/codeql.yml` | hub | (padrão) | CodeQL security scan |
| `hub/.github/workflows/claude.yml` | hub | comentário `@claude` | Claude Code Action |
| `hub/.github/workflows/claude-code-review.yml` | hub | PR | Claude review |
| `hub/.github/workflows/datadog-synthetics.yml` | hub | (config) | Datadog synthetics |
| `hub/.github/workflows/jscrambler-code-integrity.yml` | hub | (config) | Jscrambler integrity |
| `pipelines/Atlas/.github/workflows/python-package.yml` | Atlas | push/PR main | pytest em 3.9/3.10/3.11 — **sem testes significativos no repo** |
| `pipelines/Atlas/.github/workflows/python-package-conda.yml` | Atlas | push | conda env + flake8 genérico — herança do template |
| `shared/iconclass-data/.github/workflows/github-actions-releasedata.yml` | iconclass-data | workflow_dispatch | Gera SQLite; usa `actions/checkout@v3` e `upload-artifact@v3` (**DEPRECIADOS — CI quebrado na prática**) |
| `shared/iconclass-data-avmadrj/.github/workflows/github-actions-releasedata.yml` | iconclass-data-avmadrj | workflow_dispatch | idem acima, idêntico |
| — | `apps/iconocracia-companion` | — | **SEM CI** |
| — | `apps/iconocracia-space` | — | **SEM CI** (deploy pela HF direto) |
| — | `labs/iurisvision` | — | **SEM CI** |
| — | `pipelines/indexing/corpus-scout-agent` | — | **SEM CI** |
| — | `pipelines/indexing/gallica-mcp-server` | — | **SEM CI** |

**Cobertura real**: só o hub tem CI que efetivamente valida conteúdo
(shape, não completude). Todo o resto é placeholder, template ou inexistente.

## JS/TS dependency version drift matrix

| dep | companion | corpus-scout-agent | gallica-mcp-server | iurisvision |
|---|---|---|---|---|
| `name` | iconocracia-companion 1.0.0 | corpus-scout-agent 1.0.0 | gallica-mcp-server 1.0.0 | react-example 0.0.0 |
| `node engine` | — | — | `>=18` | — |
| `typescript` | — (não usa TS) | `^5.7.0` | `^5.7.2` | `~5.8.2` |
| `react` | — | — | — | `^19.0.0` |
| `vite` | — (esbuild IIFE) | — | — | `^6.2.0` |
| `esbuild` | `^0.28.0` | — | — | — |
| `wrangler` | `^4.78.0` | — | — | — |
| `@types/express` | — | — | `^5.0.6` | `^4.17.21` |
| `@types/node` | — | — | `^22.10.0` | `^22.14.0` |
| `tsx` | — | `^4.19.0` | `^4.19.2` | `^4.21.0` |
| `zod` | — | — | `^3.23.8` | — |
| `@huggingface/transformers` | `^4.1.0` | — | — | — |
| `@google/genai` | — | — | — | `^1.29.0` |
| `@anthropic-ai/claude-agent-sdk` | — | `^0.2.88` | — | — |
| `@modelcontextprotocol/sdk` | — | — | `^1.6.1` | — |

**Drift observado:**
- **TypeScript**: três versões — 5.7.0 / 5.7.2 / 5.8.2. Gap real entre
  `~5.8.2` (iurisvision) e `^5.7.0` (scout-agent). Não crítico, mas
  recomenda-se alinhar em `^5.8`.
- **@types/express**: v4 (iurisvision) vs v5 (gallica-mcp) — **major bump**.
- **@types/node**: 22.10 vs 22.14 (minor diff).
- Cada repo usa stack radicalmente diferente (esbuild+wrangler / Claude SDK
  / MCP SDK / React+Vite). **Não existe stack unificado**, o que por um
  lado é aceitável (componentes independentes), mas por outro significa
  que nenhum conjunto de tooling/linter pode ser compartilhado.

## Top-5 recomendações priorizadas

**1. [CRITICAL] Unificar `corpus-data.json`** — definir fonte canônica
única, remover cópias obsoletas (`archive/iconocracy-corpus-legacy/corpus/`),
decidir se o número canônico é 145 (AGENTS declarado) ou 165 (hub real) e
atualizar toda documentação. Criar script `tools/scripts/check_canonical_counts.py`
que falhe na CI se as 3 cópias vivas divergirem.

**2. [CRITICAL] Remover caminhos absolutos de `apps/iconocracia-companion`**
— substituir `/Users/ana/Research/hub/iconocracy-corpus/corpus/corpus-data.json`
em `scripts/sync-thesis-content.mjs` por `path.resolve(__dirname,
"../../../hub/iconocracy-corpus/corpus/corpus-data.json")` ou via env var
`CORPUS_PATH`. Mesmo fix em `seed-kv.js` (fallback `../../iconocracy-corpus/`
referencia layout flat extinto).

**3. [MAJOR] Instalar CI mínima nos 5 repos sem workflow** — companion,
iconocracia-space, iurisvision, corpus-scout-agent, gallica-mcp-server.
Template por repo: Node CI (install + build + typecheck) para JS/TS e
pytest+ruff para Python. Substituir `blank.yml` do meta-workspace por
um workflow que agregue status dos sub-repos (ou deletar o arquivo).

**4. [MAJOR] Resolver o triângulo Atlas** — consolidar as 3 cópias
(Agent 1.2 identificou `pipelines/Atlas/atlas_iconocratico/`,
`apps/iconocracia-companion/atlas-iconocratico-toolkit/atlas_iconocratico/`,
`apps/iconocracia-companion/Atlas Iconocrático/app.js`) num único pacote
em `pipelines/Atlas/` e fazer a companion consumir via npm workspace,
pip editable, ou Git submodule. Remover os forks divergentes.

**5. [MAJOR] Atualizar symlinks legados + documentar os não documentados**
— `hub/.../tools/scripts/{txt,kw,notations.txt,keys.txt}` devem apontar
para `~/Research/shared/iconclass-data/` (já existe no workspace), não
para `~/iconclass-data/`. Adicionar à tabela do AGENTS.md root: a entrada
`hub → iurisvision`, `hub → js-genai`, `rotinas/templates → hub/vault/_templates`.
Atualizar `~/AGENTS.md` pessoal para não contradizer `~/Research/AGENTS.md`.

**Bônus** — arquivar `hub/postman/` (55M sem referência operacional) e
mover `Bibliografia - Uso simbólico/` + `Notas e Textos/` para fora do
repo (já estão gitignorados, causam ruído a agentes que fazem inventário).
