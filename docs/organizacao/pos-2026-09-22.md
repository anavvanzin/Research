# Relatório pós-organização — 2026-09-22

Branch: `chore/organizacao-2026-09-22` · Ponto de restauração: commit
`7b7670bf` (inventário pré + manifesto de triagem) · Inventário pré:
`inventario-pre-2026-09-22.{json,md}`.

## Antes → depois

- Raiz: **179 entradas visíveis → 67** (57 dirs + 7 arquivos; os 7 arquivos
  restantes são todos KEEP: `AGENTS.md`, `CLAUDE.md`, `README.md`,
  `acc.db*` (live), `environment.yml`). **Zero arquivos soltos de trabalho.**
- Criadas: `iconocracia/` (30 itens), `datasets/` (8), `relatorios/` (7),
  `admin/` (5), `midia/` (4), `archive/{repos,sessoes,zips,skills}/`,
  `archive/.lixo-2026-09-22/` (quarentena).

## O que foi movido (manifestos completos em docs/organizacao/moves-*.json)

1. **Quarentena** (`archive/.lixo-2026-09-22/`, 218 MB — nada apagado,
   aguardando sua decisão final): `_apagar/`, `__pycache__/`, `cloudfare/`
   (só node_modules), `output/` (ruff caches), `Tools/`, `artigos/` (3 md de
   sessão), `payload.json`, `document_instructions_to_agent.md` + **5
   duplicatas exatas por SHA-256**: `DECISAO-METODOLOGICA… 2.md`,
   `pesquisa-padrao… 2.md`, `panofsky-heatmap… (1).xlsx`,
   `iconocracia_docs (1).zip`, `Iconocracia  arquitetura…pptx`.
2. **Arquivados** (`archive/`): 24 pastas de sessão datadas
   (`sessoes/2026-{07,08,09}/`), 10 repos (incl. `iconocracy-corpus-1`
   clone 860 MB — tudo pushed, 6 arquivos dirty preservados), 5 zips cujo
   conteúdo difere da pasta extraída (`zips/`), `ml-intern/` (descoberta:
   `.git` era stub — só `fsmonitor--daemon.ipc`; os dados de "git" no scan
   eram do repo raiz), `Iconocracia research report`, `abnt-6023` (skill).
3. **Movidos a pastas temáticas**: material da tese (`THE COIN AND THE
   WITCH`, `IDEAS 18` (Europa regina), `pacote-tese-iconocracia`,
   `reEnquadramento-tese-1822-1922-2026-09-22`, `iconocracia_junho2026-2`,
   `ARTIGOS EM DESENVOLVIMENTO`), dados de treino (`datasets/
   iconocracy-training`), relatórios de agentes (`relatorios/`), admin
   pessoal (`admin/`), mídia (`midia/`), skill órfã `SKILL.md` +
   `personality-safo.md` → `.agents/skills/ana-writing-voice/`.

## Verificações

- Symlinks (`Symlink - *`, `iconocracia-companion` → `apps/`) intactos.
- `acc.db*` intocado; nenhum path git-tracked movido.
- `git status` na raiz: apenas `docs/organizacao/` novo + `AGENTS.md`/
  `CLAUDE.md` modificados (drift pins).
- Nenhuma deleção real: quarentena revisável e reversível.

## Pendências — decisões suas (itens mantidos na raiz, ver
## moves-subrepos-2026-09-22.json "manter_report")

- **`iconocracy-editorial-wt/`** — worktree registrado do monorepo, branch
  `editorial/revisao-2026-06-24`, **22 arquivos não commitados**. Não toquei
  (quebrar o registro perderia o vínculo). Se o trabalho já foi incorporado,
  o caminho é salvá-los e `git worktree remove` de dentro de
  `hub/iconocracy-corpus`.
- **`iconocracia-cv/`** — repo da tese (anavvanzin/iconocracia-cv) com 2
  dirty: destino canônico? (`apps/`? `labs/`? manter na raiz?)
- **`imagens/`**, **`site-anavvanzin/`** — sites com remoto; o layout canônico
  de sites é `~/Projects/`. Mover para lá?
- **Dirs pessoais/espelho na raiz** (4,3 GB `Documents/`, 5,8 GB `GitHub/`,
  `Downloads/`, `projetos/`, `ana 2026 main`, `kimi/`, `INDEX/`, `YEY/`,
  `setembro/`, `binaries/`, `local/`, etc.) — não os movi por risco de
  quebrar referências de apps. Candidatos a revisão individual.
- **Pesquisa histórica**: `United-by-Marriage 2/` (328 MB, nacionalidade
  feminina — pode alimentar a tese), `o-grande-jogo-das-alegorias/` (397 MB),
  `2026-materiais/` (443 MB, disciplinas), `traducoes/`, `IDEAS 18` já foi
  p/ iconocracia.
- **PDF 172 MB** `cf0fae6e….pdf` em `iconocracia/` — nome-hash, sem
  metadados; identificar e renomear ou arquivar.
- **`pipelines/` e `deep-memory/`** — constavam no AGENTS.md/CLAUDE.md mas
  nunca existiram na raiz: corrigi os docs com drift-pins (commit desta
  branch).
- **Quarentena**: revise `archive/.lixo-2026-09-22/` e diga se esvazio
  (apago de verdade) ou mantenho.

## Drift protocol

Docs atualizados com valor real + drift-pins: `AGENTS.md` (2 pontos),
`CLAUDE.md` (workspace shape + Sibling repos). Fix commitado atomicamente
nesta branch.
